
export const isBypassRoutes=[
    "/api/polar/webhook",
    "/api/inngest(.*)",
    "/api/auth(.*)",
    "/convex(.*)"
]

export const isPUblicRoute =[
    "/auth(.*)",
    "/"
]
export const isProtectedRoute = [
    "/dashboard(.*)"
]